name: jkautomed
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
#Const is used to declare a variable whose value cannot change and is fixed at compile time.
version: 1.0.4+5

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
#final is keyword used to declare runtime final value, and const is compile time declare and it cannot be change in runtime.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  another_flushbar: ^1.12.0
  cached_network_image: ^3.3.1
  http: ^1.2.1
  shared_preferences: ^2.0.10
  flutter_widget_from_html: ^0.15.1
  qr_flutter: ^4.1.0
  intl: ^0.19.0
  google_maps_flutter: ^2.9.0
  geocoding: ^2.0.4
  geolocator: ^12.0.0
  path_provider: ^2.1.3

  custom_rating_bar: ^2.0.5
  shimmer: ^3.0.0

  razorpay_flutter: 1.4.0
  package_info_plus: ^9.0.0
  firebase_core: ^3.15.2
  firebase_messaging: ^15.2.10
  flutter_local_notifications: ^17.2.3
  flutter_cashfree_pg_sdk: ^2.2.10+48
#  cashfree_pg:

  crypto: ^3.0.6
#  new_version_plus: ^0.0.12
#  new_version_plus: 0.0.10
  url_launcher: ^6.3.1

  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5


  flutter_rating_bar: ^4.0.1
#  carousel_slider: ^4.2.1
  flutter_html: ^3.0.0
  carousel_slider: ^5.0.0

  fluttertoast:
  marquee: ^2.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.4.12
  riverpod_generator: ^2.4.0

# statelessWidget is immutable and does not change in runtime ,
# statefullWidget is mutable it is change in runtime using setstate.
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

dependency_overrides:
#  package_info_plus:

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/gradient_background.png
    - assets/images/splacelogo.png
    - assets/images/plant_vector_1.png
    - assets/images/plant_vector_2.png
    - assets/images/plant_vector_3.png
    - assets/images/plant_vector_4.png
    - assets/images/plant_vector_5.png
    - assets/images/profile_placeholder.png
    - assets/images/user_logout.png
    - assets/images/change_password_logo.png
    - assets/images/about_us_logo.png
    - assets/images/privacy_logo.png
    - assets/images/terms_logo.png
    - assets/images/bank_details_logo.png
    - assets/images/customer_home_image.jpg
    - assets/images/icon_indoor.png
    - assets/images/icon_other.png
    - assets/images/icon_office.png
    - assets/images/icon_outdoor.png
    - assets/images/bags.png
    - assets/images/organic_product.png
    - assets/images/rating_image.png
    - assets/images/photo_source_camera.png
    - assets/images/photo_source_gallery.png
    - assets/images/tick.png
    - assets/images/splaceBackground.png
    - assets/images/splacelogo.png

    - assets/icons/icon_add.png
    - assets/icons/icon_user.png
    - assets/icons/icon_notification.png
    - assets/icons/icon_home.png
    - assets/icons/icon_add_product_photo.png
    - assets/icons/protection.png
    - assets/icons/icon_camera.png
    - assets/icons/bank.png
    - assets/icons/icon_location.png
    - assets/icons/icon_shopping.png
    - assets/icons/icon_search.png
    - assets/icons/no_results.png
    - assets/icons/search_hint_icon.png
    - assets/icons/no_notification.png
    - assets/icons/empty_list.png
    - assets/icons/shopping_cart.png
    - assets/icons/out_of_cart.png
    - assets/icons/add_to_cart.png
    - assets/icons/remove_from_cart.png
    - assets/icons/buy.png
    - assets/icons/supplier.png
    - assets/icons/customer.png
    - assets/icons/locationIcon.png
    - assets/images/homeImgIcon.png
    - assets/images/homeimage.png
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Aclonica
      fonts:
        - asset: assets/fonts/Aclonica-Regular.ttf
          weight: 100
    - family: Poly
      fonts:
        - asset: assets/fonts/Poly-Regular.ttf
          weight: 100

  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
